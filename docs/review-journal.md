# Review Journal

I treated `watch-ui-table-line` as a project where the smallest useful behavior should still be inspectable.

The local checks classify each case as `ship`, `watch`, or `hold`. That gives the project a small review vocabulary that matches its frontend apps focus without claiming live deployment or external usage.

## Cases

- `baseline`: `view drift`, score 152, lane `ship`
- `stress`: `state pressure`, score 195, lane `ship`
- `edge`: `layout risk`, score 147, lane `ship`
- `recovery`: `interaction cost`, score 251, lane `ship`
- `stale`: `view drift`, score 201, lane `ship`

## Note

This file is intentionally plain so the fixture remains the source of truth.

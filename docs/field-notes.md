# Field Notes

`watch-ui-table-line` is easiest to review by starting with the fixture, not the prose.

The domain cases cover `view drift`, `state pressure`, `layout risk`, and `interaction cost`. They sit beside the smaller starter fixture so the project has both a compact scoring check and a domain-flavored review check.

The widest spread is between `interaction cost` and `layout risk`, so those are the first two cases I would preserve during a refactor.

The local verifier covers this data so the notes stay tied to code.
